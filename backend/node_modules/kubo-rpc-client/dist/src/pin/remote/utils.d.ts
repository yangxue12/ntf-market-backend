export function decodePin({ Name: name, Status: status, Cid: cid }: {
    Name: string;
    Cid: string;
    Status: import('../../types.js').Status;
}): import('../../types.js').Pin;
export function encodeService(service: any): string;
export function encodeCID(cid: any): string;
export function encodeQuery({ service, cid, name, status, all }: import('../../types.js').Query & {
    all?: boolean;
}): URLSearchParams;
export function encodeAddParams({ cid, service, background, name, origins }: import('../../types.js').AddOptions & {
    cid: CID;
}): URLSearchParams;
import { CID } from "multiformats/cid";
//# sourceMappingURL=utils.d.ts.map